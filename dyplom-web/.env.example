# Backend API (Express). No trailing slash.
VITE_API_URL=http://localhost:3000

# Opcjonalnie: ten sam klucz co w aplikacji mobilnej (Google Cloud → Maps Platform → Places API + Maps JavaScript API).
# Bez klucza pole „Miasto” używa statycznej listy polskich miast.
# VITE_GOOGLE_MAPS_API_KEY=
